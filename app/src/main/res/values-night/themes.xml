<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.YiApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/color</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>


    //Dark theme for app
    <style name = "SCREEN">
        <item name="android:background">@color/dark</item>
    </style>
    <style name ="Header">
        <item name="android:background">@color/header_dark</item>
    </style>
    <style name="Wave">
        <item name="android:background">@drawable/wave_dark</item>
        <item name="color">@color/header_dark</item>
    </style>

    //text colour if Dark mode is on
    <style name="TEXT">
        <item name="android:textColor">@color/text_colour</item>
    </style>

    //button colour if Dark mode is on
    <style name="BUTTON">
        <item name="android:background">@color/dark</item>
        <item name="android:textColor">@color/text_colour</item>
    </style>

    <style name="Card">
        <item name="android:background">@color/card_dark</item>
    </style>
</resources>